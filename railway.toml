[build]
builder = "NIXPACKS"
postBuildCommand = "curl -L $MODEL_URL -o MobileNetV2_RiceLeaf.h5"
 
[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
 